# This file was autogenerated by actions.nix. Do not edit it manually.
# To make changes, edit the workflow definition in your flake's actions-nix configuration
# (typically under flake.actions-nix.workflows.".github/workflows/conda.yaml") and run:
#   nix run .#render-workflows
# Or commit to trigger the pre-commit hook if enabled.
jobs:
  conda:
    defaults:
      run:
        shell: bash -l {0}
    runs-on: ${{ matrix.platform }}
    steps:
    - uses: actions/checkout@v6
    - uses: mamba-org/setup-micromamba@v2
      with:
        cache-downloads: true
        cache-environment: true
        create-args: python=${{ matrix.python-version }}
        environment-file: environment.yaml
        init-shell: bash powershell
        micromamba-version: 1.5.6-0
        post-cleanup: all
    - run: |-
        echo "Testing package import"
        python -c 'import fractopo'
        echo "Testing module entrypoint"
        python -m fractopo --help
        echo "Running unittests with pytest"
        pytest -v
    strategy:
      fail-fast: false
      matrix:
        platform:
        - ubuntu-latest
        - macos-latest
        - windows-latest
        python-version:
        - '3.10'
        - '3.11'
        - '3.12'
        - '3.13'
        - '3.14'
    timeout-minutes: 30
'on':
  push:
    paths:
    - fractopo/**.py
    - tests/**.py
    - .github/workflows/conda.yaml
    - pyproject.toml
    - environment.yaml
  workflow_dispatch: {}
